---
// DeviceBento.astro - Jerarquía Matemática y Contraste Reforzado
---

<section id="device-selector" class="device-bento-section">
  <div class="container-pipod">
    <div class="header-wrapper">
      <h2 class="section-heading">¿Qué equipo vamos a recuperar hoy?</h2>
    </div>
    
    <div class="bento-grid">
      <div class="bento-card featured-blue macbook-card">
        <div class="card-content">
          <div class="top-content">
            <span class="badge-pill-solid">ESPECIALISTAS M1 · M4</span>
            <div class="text-group">
              <h3 class="highlight-title">MacBook</h3>
              <p class="legibilidad-max">Arquitectura Logic Board y microsoldadura de precisión.</p>
            </div>
          </div>
          <div class="button-wrapper">
            <a href="/macbook" class="btn-pipod-white">Diagnóstico Pro</a>
          </div>
        </div>
      </div>

      <div class="bento-card featured-black iphone-card">
        <div class="card-content">
          <div class="top-content">
            <span class="badge-pill-solid-dark">EXPRESS 2H</span>
            <div class="text-group">
              <h3 class="highlight-title">iPhone</h3>
              <p class="legibilidad-max">Pantalla, batería y FaceID en tiempo récord.</p>
            </div>
          </div>
          <div class="button-wrapper">
            <a href="/iphone" class="btn-pipod-outline">Agendar</a>
          </div>
        </div>
      </div>

      <div class="bento-card neutral-grey">
        <div class="card-content">
          <div class="top-content">
            <div class="header-small">
               <i class="bi bi-tablet"></i>
               <h4>iPad</h4>
            </div>
            <p class="desc-small">Cristales y LCD técnica original.</p>
          </div>
          <span class="price-mono">DESDE $45K</span>
        </div>
      </div>

      <div class="bento-card neutral-grey">
        <div class="card-content">
          <div class="top-content">
            <div class="header-small">
               <i class="bi bi-watch"></i>
               <h4>Watch</h4>
            </div>
            <p class="desc-small">Restauración de cristal Series/Ultra.</p>
          </div>
          <span class="price-mono">DESDE $28K</span>
        </div>
      </div>
    </div>
  </div>

  <div class="gema-incentive-strip">
    <div class="gema-container">
      <div class="gema-item"><i class="bi bi-shield-check"></i> <span>Garantía de 1 año</span></div>
      <div class="gema-divider"></div>
      <div class="gema-item"><i class="bi bi-truck"></i> <span>Recogida a domicilio</span></div>
      <div class="gema-divider"></div>
      <div class="gema-item"><i class="bi bi-lightning-charge"></i> <span>Reparación Express</span></div>
    </div>
  </div>
</section>

<style>
  * {
    box-sizing: border-box;
  }

  .device-bento-section {
    background-color: #FFFFFF;
    padding: 80px 0;
    border-top: 1px solid #E0E0E0;
  }

  .container-pipod {
    max-width: 1440px;
    margin: 0 auto;
    padding: 0 80px;
  }

  .header-wrapper {
    text-align: left;
    margin-bottom: 50px;
  }

  .section-heading {
    font-family: 'Inter', sans-serif;
    font-size: clamp(1.5rem, 5vw, 2.5rem);
    font-weight: 900;
    letter-spacing: -1px;
    text-transform: uppercase;
    margin: 0;
    color: #000000;
  }

  .bento-grid {
    display: grid;
    grid-template-columns: 1.8fr 1.4fr 1fr 1fr;
    gap: 20px;
    margin-bottom: 80px;
  }

  .macbook-card {
    min-height: 420px;
  }

  .iphone-card {
    min-height: 400px;
  }

  .bento-card {
    min-height: 380px;
    border-radius: 28px;
    padding: 32px;
    display: flex;
    flex-direction: column;
    transition: all 0.5s cubic-bezier(0.165, 0.84, 0.44, 1);
    border: 1px solid rgba(0,0,0,0.05);
    overflow: hidden;
  }

  .bento-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 20px 40px rgba(0,0,0,0.1);
  }

  .card-content {
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    gap: 20px;
    height: 100%;
  }

  .top-content {
    display: flex;
    flex-direction: column;
    gap: 16px;
  }

  .button-wrapper {
    display: flex;
    justify-content: flex-end;
  }

  .macbook-card .highlight-title {
    font-size: 28px;
  }

  .iphone-card .highlight-title {
    font-size: 26px;
  }

  .macbook-card .legibilidad-max {
    font-size: 15px;
  }

  .iphone-card .legibilidad-max {
    font-size: 14px;
  }

  .featured-blue {
    background: #3A506B;
    color: white;
    border: none;
  }

  .featured-black {
    background: #000000;
    color: white;
    border: none;
  }

  .neutral-grey {
    background-color: #F5F5F7;
    color: #1D1D1F;
  }

  .legibilidad-max {
    color: #FFFFFF;
    font-size: 14px;
    line-height: 1.5;
    margin: 0;
    opacity: 0.95;
    font-weight: 400;
  }

  .text-group {
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .badge-pill-solid {
    background: #FFFFFF;
    color: #3A506B;
    font-size: 10px;
    font-weight: 700;
    padding: 6px 12px;
    border-radius: 50px;
    display: inline-block;
    width: fit-content;
  }

  .badge-pill-solid-dark {
    background: #3A506B;
    color: #FFFFFF;
    font-size: 10px;
    font-weight: 700;
    padding: 6px 12px;
    border-radius: 50px;
    display: inline-block;
    width: fit-content;
  }

  .btn-pipod-white {
    background: #FFFFFF;
    color: #000;
    padding: 11px 22px;
    border-radius: 50px;
    text-decoration: none;
    font-weight: 700;
    font-size: 13px;
    border: none;
    cursor: pointer;
    font-family: 'Inter', sans-serif;
    transition: all 0.3s ease;
    width: fit-content;
  }

  .btn-pipod-white:hover {
    background: #f0f0f0;
    transform: translateY(-2px);
  }

  .btn-pipod-outline {
    border: 2px solid #FFFFFF;
    color: #FFFFFF;
    padding: 10px 20px;
    border-radius: 50px;
    text-decoration: none;
    font-weight: 700;
    font-size: 13px;
    background: transparent;
    cursor: pointer;
    font-family: 'Inter', sans-serif;
    transition: all 0.3s ease;
    width: fit-content;
  }

  .btn-pipod-outline:hover {
    background: rgba(255,255,255,0.1);
    transform: translateY(-2px);
  }

  .gema-incentive-strip {
    width: 100%;
    height: 119px;
    background: #000000;
    display: flex;
    align-items: center;
    border-top: 1px solid rgba(255,255,255,0.1);
  }

  .gema-container {
    max-width: 1440px;
    margin: 0 auto;
    padding: 0 80px;
    width: 100%;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .gema-item {
    display: flex;
    align-items: center;
    gap: 15px;
    color: #FFFFFF;
    font-weight: 600;
    text-transform: uppercase;
    font-size: 13px;
    letter-spacing: 1px;
    font-family: 'Inter', sans-serif;
  }

  .gema-item i {
    color: #FFFFFF;
    font-size: 20px;
  }

  .gema-divider {
    width: 1px;
    height: 40px;
    background: rgba(255,255,255,0.2);
  }

  .header-small {
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .header-small i {
    font-size: 24px;
    color: #3A506B;
  }

  .header-small h4 {
    font-weight: 800;
    font-size: 18px;
    margin: 0;
    font-family: 'Inter', sans-serif;
  }

  .desc-small {
    font-size: 13px;
    color: #6e6e73;
    line-height: 1.4;
    margin: 0;
    font-family: 'Inter', sans-serif;
  }

  .price-mono {
    font-family: 'PT Mono', monospace;
    font-weight: 700;
    color: #3A506B;
    font-size: 12px;
  }

  @media (max-width: 1200px) {
    .bento-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 768px) {
    .device-bento-section {
      padding: 50px 0;
    }

    .container-pipod {
      padding: 0 20px;
    }

    .section-heading {
      font-size: 1.5rem;
      margin-bottom: 30px;
    }

    .bento-grid {
      grid-template-columns: 1fr;
      gap: 16px;
      margin-bottom: 50px;
    }

    .bento-card {
      min-height: auto;
      padding: 24px;
    }

    .gema-incentive-strip {
      height: auto;
      padding: 30px 0;
    }

    .gema-container {
      flex-direction: column;
      gap: 20px;
      padding: 0 20px;
    }

    .gema-divider {
      display: none;
    }

    .gema-item {
      font-size: 12px;
      gap: 10px;
    }
  }
</style>