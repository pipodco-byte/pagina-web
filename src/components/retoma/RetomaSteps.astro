---
/**
 * DATA: Pasos del Plan Retoma
 */
import { STEPS } from '../../data/constants';
---

<section id="como-funciona" class="steps-section">
  <div class="container">
    
    <header class="header-center">
      <h2 style="font-family: 'Inter', sans-serif; font-size: 2.8rem; font-weight: 800; margin: 0 0 40px; text-align: left;">TRES PASOS POR TU NUEVO EQUIPO</h2>
    </header>

    <div class="steps-grid">
      {STEPS.map((step) => (
        <article class="step-item">
          <div class="step-number-wrapper">
            <span class="step-number">{step.number}</span>
          </div>
          <div class="step-content">
            <h3>{step.title}</h3>
            <p>{step.desc}</p>
          </div>
        </article>
      ))}
    </div>

  </div>
</section>

<style>
  @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700;800;900&family=PT+Mono:wght@400;700&family=Noto+Sans:wght@400;500;600&display=swap');

  /* CONFIGURACIÓN BLANCO Y NEGRO PREMIUM */
  .steps-section {
    background-color: #FFFFFF; /* Blanco Perfecto */
    padding: 50px 0;
    font-family: 'Inter', system-ui, sans-serif;
  }

  .container {
    max-width: 1440px;
    margin: 0 auto;
    padding: 0 25px;
  }

  @media (min-width: 1024px) {
    .steps-section {
      padding: 80px 0;
    }

    .container {
      padding: 0 80px;
    }
  }

  /* HEADER CENTRADO */
  .header-center {
    text-align: center;
    margin-bottom: 80px;
  }

  .eyebrow {
    display: none;
  }

  .main-title {
    display: none;
  }

  .main-description {
    display: none;
  }

  /* GRID DE PASOS */
  .steps-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 60px;
  }

  @media (min-width: 768px) {
    .steps-grid {
      grid-template-columns: repeat(3, 1fr);
      gap: 40px;
    }
  }

  /* ITEM DE PASO */
  .step-item {
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    transition: transform 0.4s ease;
  }

  .step-item:hover {
    transform: translateY(-5px);
  }

  /* NÚMEROS EN NEGRO SÓLIDO */
  .step-number-wrapper {
    margin-bottom: 24px;
  }

  .step-number {
    font-size: clamp(4rem, 8vw, 5.5rem);
    font-weight: 900;
    line-height: 1;
    color: #000000; /* Negro sólido como pediste */
    letter-spacing: -0.05em;
  }

  /* CONTENIDO DE TEXTO */
  .step-content h3 {
    font-family: 'Inter', sans-serif;
    font-size: 1.75rem;
    font-weight: 800;
    color: #1D1D1F;
    margin-bottom: 12px;
    letter-spacing: -0.02em;
  }

  .step-content p {
    font-family: 'Noto Sans', sans-serif;
    font-size: 1rem;
    line-height: 1.6;
    color: #424245; /* Gris oscuro para legibilidad */
    max-width: 280px;
    margin: 0 auto;
  }
</style>