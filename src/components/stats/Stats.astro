<section class="stats-section">
  <div class="stats-container">
    <div class="stats-grid">
      
      <div class="stat-item">
        <div class="number-wrapper">
          <h2 class="stat-number" data-target="15">0</h2>
          <span class="stat-suffix">años</span>
        </div>
        <p class="stat-text">En el mercado</p>
      </div>

      <div class="stat-item separator">
        <div class="number-wrapper">
          <h2 class="stat-number" data-target="3600">0</h2>
          <span class="stat-suffix">+</span>
        </div>
        <p class="stat-text">Equipos reparados</p>
      </div>

      <div class="stat-item">
        <div class="number-wrapper">
          <h2 class="stat-number" data-target="2500">0</h2>
          <span class="stat-suffix">+</span>
        </div>
        <p class="stat-text">Clientes satisfechos</p>
      </div>

    </div>
  </div>
</section>

<script>
  // Script de animación idéntico pero optimizado para carga rápida
  const statsObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        const target = entry.target;
        const targetValue = parseInt(target.getAttribute('data-target'));
        let current = 0;
        const increment = targetValue / 100;
        const updateCount = () => {
          if (current < targetValue) {
            current += increment;
            target.innerText = Math.ceil(current);
            setTimeout(updateCount, 15);
          } else {
            target.innerText = targetValue;
          }
        };
        updateCount();
        statsObserver.unobserve(target);
      }
    });
  }, { threshold: 0.5 });
  document.querySelectorAll('.stat-number').forEach(stat => statsObserver.observe(stat));
</script>

<style>
  .stats-section {
    background-color: #000;
    /* Ajuste de Altura: 119px totales */
    height: 119px;
    width: 100%;
    display: flex;
    align-items: center;
    overflow: hidden;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  }

  .stats-container {
    /* Ajuste de Ancho: Máximo 1440px */
    max-width: 1440px;
    width: 100%;
    margin: 0 auto;
    padding: 0 40px;
  }

  .stats-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    align-items: center;
  }

  .stat-item {
    text-align: center;
    display: flex;
    flex-direction: row; /* Cambio a horizontal para ahorrar altura */
    align-items: baseline;
    justify-content: center;
    gap: 15px;
  }

  .number-wrapper {
    display: flex;
    align-items: baseline;
  }

  .stat-number {
    /* Tamaño ajustado para encajar en 119px de alto */
    font-size: 2.5rem; 
    font-weight: 800;
    letter-spacing: -1px;
    font-family: 'Inter', sans-serif;
    color: #ffffff;
    margin: 0;
  }

  .stat-suffix {
    font-size: 1rem;
    font-family: 'PT Mono', monospace;
    font-weight: 500;
    color: rgba(255, 255, 255, 0.7);
    margin-left: 8px;
    text-transform: uppercase;
  }

  .stat-text {
    font-size: 0.85rem;
    font-family: 'Inter', sans-serif;
    font-weight: 400;
    color: rgba(255, 255, 255, 0.5);
    margin: 0;
    letter-spacing: 1px;
    text-transform: uppercase;
  }

  /* Separadores delgados */
  .separator {
    border-left: 1px solid rgba(255, 255, 255, 0.1);
    border-right: 1px solid rgba(255, 255, 255, 0.1);
  }

  /* Responsive: Se apila pero mantiene la sobriedad */
  @media (max-width: 768px) {
    .stats-section {
      height: auto;
      padding: 40px 0;
    }
    .stats-grid { grid-template-columns: 1fr; gap: 30px; }
    .separator { border: none; }
  }
</style>