---
import Layout from '../layouts/Layout.astro';
import LocalBusinessSchema from '../components/SEO/LocalBusinessSchema.astro';
import '../../assets/scss/astro-ecommerce.scss';
import Footer from '../components/pipodFooter';
import FloatingContact from '../layouts/floatingContact';
import CardProduct from '../components/products/cardProduct';
import ServiceBentoDark from '../components/serviceCard';
import IncentiveShortBlackA from '../components/incentives/incentiveShortBlackA';
import ReviewWall from '../components/promo/pipodGoogleReviews.jsx';
import BlogSection from '../components/reviews/blogSection';
import Stats from '../components/stats/pipodStats.astro';
import HeroBentoCarousel from '../components/hero/heroBentoCarousel.astro';
import PaymentBanner from '../components/payment/PaymentBanner.astro';
import ServiceCardsSection from '../components/home/ServiceCardsSection.astro';
import ProductsIphoneSection from '../components/home/ProductsIphoneSection.astro';
import ProductsMacbooksSection from '../components/home/ProductsMacbooksSection.astro';
import { SectionTracker } from '../components/gtm';
import { getProductos } from '../lib/contentful';

const productos = await getProductos();
---

<Layout title="Pipod - Servicio Técnico Apple Especializado" description="Servicio técnico Apple especializado en Bogotá. Reparación de MacBook, iPhone, iPad. Baterías, pantallas y microsoldadura con garantía de 12 meses.">
  <LocalBusinessSchema />
  <main>
    
    <!-- Hero Carousel Section -->
    <HeroBentoCarousel />

    <!-- Stats Section -->
    <Stats />

    <!-- Core Services (3 Pillars) -->
    <ServiceCardsSection />

    <!-- Why Choose Us (Differentiators) -->
    <IncentiveShortBlackA client:load />

    <!-- Featured Products (iPhones) -->
    <ProductsIphoneSection productos={productos} />

    <!-- Payment Methods Banner -->
    <PaymentBanner />

    <!-- Featured Products (MacBooks) -->
    <ProductsMacbooksSection productos={productos} />

    <!-- Social Proof (Testimonials) -->
    <ReviewWall
      pageHeaderBgImg="https://images.unsplash.com/photo-1531297484001-80022131f5a1?ixlib=rb-4.0.3&auto=format&fit=crop&w=2070&q=80"
      pageHeaderMinVh="40vh"
    />

    <!-- Blog/News Section -->
    <BlogSection client:load />

<FloatingContact client:load />
    <Footer client:load />
  </main>
</Layout>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const carouselEl = document.getElementById('heroCarousel');
    if (carouselEl) {
      let currentIndex = 0;
      const items = carouselEl.querySelectorAll('.carousel-item');
      
      setInterval(() => {
        items[currentIndex].classList.remove('active');
        currentIndex = (currentIndex + 1) % items.length;
        items[currentIndex].classList.add('active');
      }, 5000);
    }
  });
</script>
