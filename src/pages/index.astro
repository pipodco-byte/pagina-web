---
import Layout from '../layouts/Layout.astro';
import '../../assets/scss/astro-ecommerce.scss';
import PipodNavbar from '../components/pipodNavbar.astro';
import Footer from '../components/footer';
import CardProduct from '../components/products/cardProduct';
import IncentiveCols from '../components/incentives/incentiveCols';
import TestimonialsFade from '../components/promo/testimonialsFade';
import Stats from '../components/stats/Stats.astro';
import { getProductos } from '../lib/contentful';

const productos = await getProductos();
const iphones = productos.filter(p => p.nombre.toLowerCase().includes('iphone'));
const macbooks = productos.filter(p => p.nombre.toLowerCase().includes('macbook'));
---

<Layout title="Pipod - Servicio Técnico Apple Especializado">
  <main style="padding-bottom: 80px;">
    <PipodNavbar />
    
    <!-- Hero Carousel Section -->
    <section class="position-relative overflow-hidden">
      <div id="heroCarousel" class="carousel slide" data-bs-ride="carousel">
        <div class="carousel-inner">
          <div class="carousel-item active">
            <div class="bg-gradient-primary text-white py-20">
              <div class="container text-center">
                <h1 class="display-4 font-weight-bold mb-4">Servicio Técnico Especializado Apple en Bogotá</h1>
                <p class="lead mb-6">Diagnóstico preciso y repuestos de calidad</p>
                <a href="/contacto" class="btn btn-white btn-lg px-6 py-3">AGENDA TU CITA</a>
              </div>
            </div>
          </div>
          <div class="carousel-item">
            <div class="bg-gradient-success text-white py-20">
              <div class="container text-center">
                <h1 class="display-4 font-weight-bold mb-4">¡50% de Descuento en Mantenimiento MacBook!</h1>
                <p class="lead mb-6">Usa código Pipod20Web</p>
                <a href="/contacto" class="btn btn-white btn-lg px-6 py-3">Agenda Ahora</a>
              </div>
            </div>
          </div>
          <div class="carousel-item">
            <div class="bg-gradient-info text-white py-20">
              <div class="container text-center">
                <h1 class="display-4 font-weight-bold mb-4">Plan Retoma</h1>
                <p class="lead mb-6">Cambia tu viejo equipo por uno nuevo</p>
                <a href="/productos?categoria=plan-retoma" class="btn btn-white btn-lg px-6 py-3">Saber Más</a>
              </div>
            </div>
          </div>
        </div>
        <button class="carousel-control-prev" type="button" data-bs-target="#heroCarousel" data-bs-slide="prev">
          <span class="carousel-control-prev-icon"></span>
        </button>
        <button class="carousel-control-next" type="button" data-bs-target="#heroCarousel" data-bs-slide="next">
          <span class="carousel-control-next-icon"></span>
        </button>
      </div>
    </section>

    <!-- Core Services (3 Pillars) -->
    <section class="py-12 bg-light">
      <div class="container">
        <div class="row">
          <div class="col-md-4 mb-6">
            <div class="card h-100 shadow">
              <div class="card-body text-center p-6">
                <div class="mb-4">🔧</div>
                <h4 class="card-title">Reparación de Equipos</h4>
                <p class="card-text">Diagnóstico rápido y garantía en todas nuestras reparaciones</p>
                <a href="/servicio-tecnico" class="btn btn-primary">AGENDA</a>
              </div>
            </div>
          </div>
          <div class="col-md-4 mb-6">
            <div class="card h-100 shadow">
              <div class="card-body text-center p-6">
                <div class="mb-4">🔄</div>
                <h4 class="card-title">Plan Retoma</h4>
                <p class="card-text">Cambia lo viejo por lo nuevo con nuestro plan de intercambio</p>
                <a href="/productos?categoria=plan-retoma" class="btn btn-primary">Saber Más</a>
              </div>
            </div>
          </div>
          <div class="col-md-4 mb-6">
            <div class="card h-100 shadow">
              <div class="card-body text-center p-6">
                <div class="mb-4">🛍️</div>
                <h4 class="card-title">Accesorios</h4>
                <p class="card-text">Accesorios originales y compatibles para todos tus dispositivos</p>
                <a href="/productos?categoria=accesorios" class="btn btn-primary">IR A TIENDA</a>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Featured Products (iPhones) -->
    <section class="py-12">
      <div class="container">
        <h2 class="text-center mb-8 display-6">Los iPhones más Populares</h2>
        <div class="row">
          {iphones.map(producto => (
            <div class="col-md-6 col-lg-3 mb-4">
              <CardProduct 
                thumb_src={producto.thumb_src}
                thumb_alt={producto.thumb_alt}
                color={producto.color}
                colors={producto.colors}
                title={producto.title}
                description={producto.description}
                price={producto.price}
                position="center"
              />
            </div>
          ))}
        </div>
      </div>
    </section>
    <!-- Payment Methods Banner -->
    <section class="payment-section py-5">
      <div class="container">
        <div class="row justify-content-center align-items-center">
          <div class="col-12 text-center mb-5">
            <span class="trust-badge">Pagos procesados por tecnología de punta</span>
          </div>
          
          <div class="payment-grid">
            <div class="payment-item">
              <img src="https://bold.co/assets/img/bold-logo.svg" alt="Bold" class="logo-real bold">
            </div>

            <div class="payment-item">
              <img src="https://www.pse.com.co/wps/wcm/connect/6014603e-1084-48f1-a3f2-89262174620a/logo-pse.svg?MOD=AJPERES" alt="PSE" class="logo-real pse">
            </div>

            <div class="payment-item">
              <img src="https://cdn.simpleicons.org/visa/white" alt="Visa" class="logo-real">
            </div>

            <div class="payment-item">
              <img src="https://cdn.simpleicons.org/mastercard/white" alt="Mastercard" class="logo-real">
            </div>

            <div class="payment-item">
              <img src="https://cdn.simpleicons.org/americanexpress/white" alt="Amex" class="logo-real">
            </div>
          </div>
        </div>
      </div>
    </section>

    <style>
      .payment-section {
        background-color: #000;
        padding: 60px 0;
      }

      .trust-badge {
        color: rgba(255, 255, 255, 0.4);
        text-transform: uppercase;
        font-size: 0.75rem;
        letter-spacing: 4px;
        font-weight: 600;
      }

      .payment-grid {
        display: flex;
        justify-content: center;
        align-items: center;
        flex-wrap: wrap;
        gap: 3rem;
      }

      .logo-real {
        height: 30px;
        width: auto;
        filter: brightness(0) invert(1);
        opacity: 0.4;
        transition: all 0.5s cubic-bezier(0.16, 1, 0.3, 1);
        object-fit: contain;
      }

      .logo-real.pse {
        height: 48px;
      }

      .logo-real.bold {
        height: 24px;
      }

      .payment-item:hover .logo-real {
        opacity: 1;
        filter: brightness(1) invert(0);
        transform: scale(1.1);
      }

      @media (max-width: 768px) {
        .payment-grid { gap: 2rem; }
        .logo-real { height: 22px; }
        .logo-real.pse { height: 35px; }
      }
    </style>


    <!-- Featured Products (MacBooks) -->
    <section class="py-12">
      <div class="container">
        <h2 class="text-center mb-8 display-6">Laptops Potentes (MacBooks)</h2>
        <div class="row">
          {macbooks.map(producto => (
            <div class="col-md-6 col-lg-3 mb-4">
              <CardProduct 
                thumb_src={producto.thumb_src}
                thumb_alt={producto.thumb_alt}
                color={producto.color}
                colors={producto.colors}
                title={producto.title}
                description={producto.description}
                price={producto.price}
                position="center"
              />
            </div>
          ))}
        </div>
      </div>
    </section>

    <!-- Social Proof (Testimonials) -->
    <TestimonialsFade 
      pageHeaderBgImg="https://images.unsplash.com/photo-1531297484001-80022131f5a1?ixlib=rb-4.0.3&auto=format&fit=crop&w=2070&q=80"
      pageHeaderMinVh="40vh"
    />

    <!-- Why Choose Us (Differentiators) -->
    <IncentiveCols />

    <!-- Stats Section -->
    <Stats />

    <!-- Blog/News Section -->
    <section class="py-12 bg-light">
      <div class="container">
        <h2 class="text-center mb-8 display-6">Noticias y Consejos de Expertos Apple</h2>
        <div class="row">
          <div class="col-md-4 mb-4">
            <div class="card h-100 shadow-sm">
              <div class="card-body">
                <h5 class="card-title">Cómo mantener tu iPhone en perfecto estado</h5>
                <p class="card-text text-muted">Consejos prácticos para prolongar la vida útil de tu dispositivo...</p>
                <a href="#" class="btn btn-sm btn-outline-primary">Leer más</a>
              </div>
            </div>
          </div>
          <div class="col-md-4 mb-4">
            <div class="card h-100 shadow-sm">
              <div class="card-body">
                <h5 class="card-title">MacBook: Rendimiento y productividad</h5>
                <p class="card-text text-muted">Descubre cómo optimizar tu MacBook para máximo rendimiento...</p>
                <a href="#" class="btn btn-sm btn-outline-primary">Leer más</a>
              </div>
            </div>
          </div>
          <div class="col-md-4 mb-4">
            <div class="card h-100 shadow-sm">
              <div class="card-body">
                <h5 class="card-title">Reparaciones comunes en equipos Apple</h5>
                <p class="card-text text-muted">Conoce las reparaciones más frecuentes y cómo prevenirlas...</p>
                <a href="#" class="btn btn-sm btn-outline-primary">Leer más</a>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Sticky CTA (Mobile) -->
    <div class="fixed-bottom d-block d-lg-none bg-primary p-3" style="z-index: 1030;">
      <div class="container">
        <a href="/contacto" class="btn btn-white w-100 font-weight-bold">
          AGENDA TU CITA
        </a>
      </div>
    </div>

    <Footer />
  </main>
</Layout>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    const carouselEl = document.getElementById('heroCarousel');
    if (carouselEl) {
      let currentIndex = 0;
      const items = carouselEl.querySelectorAll('.carousel-item');
      
      setInterval(() => {
        items[currentIndex].classList.remove('active');
        currentIndex = (currentIndex + 1) % items.length;
        items[currentIndex].classList.add('active');
      }, 5000);
    }
  });
</script>
